# Core framework
fastapi
uvicorn[standard]

# OpenAI + Kern voice
openai>=1.0.0
python-dotenv
PyYAML
python-gnupg

# Async requests / fallback utilities
httpx
python-multipart

# Optional: Local storage & templating (keep if still in use)
tinydb
Jinja2
Flask

# Bonus
pytz
pendulum
rich
watchdog
